{
  "name": "errorfinder-eval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Adversarial evaluation harness for the Errorfinder verification pipeline.",
  "scripts": {
    "start": "tsx --tsconfig tsconfig.json src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
